# GitHub OAuth credentials. Set these to enable real GitHub sign-in.
# If left empty or unset, the system defaults to Developer Mock OAuth.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=archforge
DB_SSLMODE=disable

